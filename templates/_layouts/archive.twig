{# -- Base Page Layout -- #}
{# ----------------------------------------------------------------- #}
{% extends '_base/body.twig' %}

{# -- Page Variables -- #}
{# ----------------------------------------------------------------- #}
{% set headline = post.meta('post_headline')|default(post.title) %}
{% set subheadline = post.meta('post_subheadline')|default(null) %}
{% set bannerImage = post.meta('thumbnail')|default(null) %}
{% set bannerType = post.meta('banner_type')|default('default') %}
{% set button = post.meta('banner_button')|default(null) %}
{% if 'post-type-archive-design' in body_class %}
    {% set listType = 'design' %}
{% else %}
    {% set listType = 'posts' %}
{% endif %}

{# -- Page Templates -- #}
{# ----------------------------------------------------------------- #}
{% block content %}

    <article class="o-page">

        {% include '_components/banner.twig' with {
            params: {
                headline: title,
                text: subheadline,
                style: null,
                image: null,
                button: null
            }
        } only %}

        {% if post.content is not empty %}
            <section class="c-intro" data-scroll-trigger>
                <div class="u-wysiwyg">
                    {{ post.content }}
                </div>
            </section>
        {% endif %}

        <section class="o-section is-list" data-scroll-trigger>
            {% include '_components/list.twig' with {
                style: listType,
                params: {
                    items: posts,
                }
            } %}
        </section>
        {# <section class="c-blog">
            {% for post in posts %}
                <div class="c-blog__post">

                    {% if post.thumbnail %}
                        {% include '_elements/picture.twig' with {
                            params: {
                                src: get_image(post.thumbnail),
                                ratio: '4:3',
                                classes: 'c-member__logo'
                            }
                        } only %}

                    {% else %}
                        <picture class="c-media u-ratio u-16:9">
                            <img src="https://michiganciders.com/wp-content/themes/pomich/assets/img/placeholder.png" alt="placeholder image" class="c-media__image" />
                        </picture>

                    {% endif %}

                    <div class="u-wysiwyg">
                        <h3 class="u-heading">
                            <a href="{{ post.link }}" title="{{ post.post_headline|default(post.title) }}" class="u-link">{{ post.post_headline|default(post.title) }}</a>
                        </h3>
                        <time datetime="{{ post.date|date('c') }}">Posted on {{ post.date }}</time>
                        <p>{{ post.content|excerpt(40) }}</p>
                    </div>

                </div>
            {% endfor %}
        </section> #}

        {% include '_components/pagination.twig' with {
            params: {
                pagination: pagination
            }
        } %}

        {% if relatedPosts is not empty %}
            <footer class="o-page__related has-{{ relatedPosts|length }}-columns">
                <h3 class="u-subheadline">Related Reading</h3>
                {% include '_components/related-posts.twig' with {
                    related: relatedPosts
                } only %}
            </footer>
        {% endif %}

    </article>

    {# Sidebar #}
    {% include '_partials/sidebar.twig' with {
        params: {
            widgets: sidebar
        }
    } only %}

{% endblock %}
