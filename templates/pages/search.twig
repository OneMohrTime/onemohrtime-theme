{% extends "base.twig" %}

{% block content %}

    {% include 'blocks/page-header.twig' %}

    {% if posts == [] %}
        {{ __('Sorry, no results were found.', 'onemohrtime') }}
        {{ function('get_search_form') }}
    {% endif %}

    {% for post in posts %}
        {% include "templates/blocks/content.twig" with {post: post}  %}
    {% endfor %}

    {{ function('the_posts_navigation') }}

{% endblock %}
